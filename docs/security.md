# Security Rules

## What
Rules to protect system from hacks and leaks.

## Why
We handle sensitive vendor documents.

## Rules
- Never expose secrets in code
- Use JWT authentication
- Only allow PDF uploads
- Limit file size (10MB max)
- Encrypt sensitive data

## Access levels
- admin → full access
- reviewer → approve documents
- user → upload only